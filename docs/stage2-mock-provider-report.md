# 阶段2 Mock Provider 截图反查报告

> 日期：2026-08-06
> 范围：阶段2截图反查最小闭环、文件上传、拖拽/粘贴截图、批量截图上传、批量截图自动匹配候选视频、高分匹配一键收藏、基础鲁棒性测试、批量候选匹配、局部精排、阈值 UI、匹配帧收藏、统一项目素材视图、筛选排序和导出
> 边界：继续使用 Mock OCR 与 Mock 视觉向量；未接入真实 OCR/CLIP；未做真实平台登录、真实平台搜索或第三方视频采集。

## 已完成

- 新增 `media_assets` 数据表，保存截图分析结果。
- 新增 `frame_matches` 数据表，保存候选视频匹配结果。
- 新增 `project_frame_matches` 数据表，把截图反查匹配帧收藏到项目。
- 新增截图分析核心：格式/大小校验、FFmpeg 读取图片尺寸、生成缩略图、计算 64 位感知哈希、Mock OCR、Mock 视觉向量。
- 新增候选视频匹配核心：FFmpeg 抽帧、逐帧 pHash/Mock embedding 打分、输出最佳时间点、相似度、匹配帧路径和证据。
- 新增局部精排：先用低 fps 粗扫，再围绕最佳时间点用更高 fps 二次抽帧，证据里记录粗扫帧数、精排帧数、候选 Top5。
- 新增批量候选匹配：一次传入多个本地视频路径，成功结果按综合分排序，失败路径单独返回错误，不影响其他候选继续匹配。
- 新增批量截图自动匹配：一次传入多张已分析截图和一批候选视频路径，逐张截图跑候选匹配，并汇总每张截图的最佳匹配。
- 新增文件上传保存逻辑：上传截图保存到 `.local-data/stage2/uploads`，自动做文件名净化、扩展名校验、空文件校验和 20MB 限制。
- 新增批量截图上传逻辑：单次最多 20 张，成功和失败分开返回，坏图不会拖垮整批。
- 新增匹配帧收藏逻辑：可将当前匹配结果保存到项目，并记录标签、备注、人工确认状态、时间点和本地匹配帧路径。
- 新增高分匹配一键收藏：可按当前阈值批量收藏匹配结果，并自动跳过低分结果和已收藏结果。
- 新增统一项目素材视图：把阶段1搜索收藏和阶段2截图反查收藏合并为同一个项目素材列表，并保留来源类型。
- 统一项目素材视图支持按来源类型筛选、按时间/标题/匹配分排序，并支持 CSV/JSON/Markdown 导出。
- 新增 FastAPI 接口：
  - `POST /api/assets/analyze-image`
  - `POST /api/assets/upload-image`
  - `POST /api/assets/upload-images`
  - `GET /api/assets/{assetId}`
  - `POST /api/matches/find`
  - `POST /api/matches/batch`
  - `POST /api/matches/batch-assets`
  - `GET /api/matches/{matchId}`
  - `POST /api/projects/{projectId}/frame-matches`
  - `POST /api/projects/{projectId}/frame-matches/batch`
  - `GET /api/projects/{projectId}/frame-matches`
  - `GET /api/projects/{projectId}/library`
  - `GET /api/projects/{projectId}/library.{fmt}`
- 桌面端“截图反查”面板新增文件选择器，可以直接上传并分析截图，也保留本地路径分析入口。
- 桌面端“截图反查”面板新增拖拽截图和剪贴板粘贴截图入口，三种上传方式复用同一条分析流程。
- 桌面端文件选择和拖拽支持一次上传多张截图，批量结果会展示成功资产和失败原因。
- 桌面端新增“批量截图匹配候选视频”按钮，可把当前批量上传成功的截图逐张匹配同一批候选视频。
- 桌面端错误提示优化：优先展示本地 API 返回的 `detail`，避免直接显示整段 JSON。
- 桌面端新增批量候选视频路径输入框，每行一个视频路径。
- 桌面端新增匹配阈值、粗扫 FPS、精排 FPS、精排窗口参数输入。
- 桌面端新增“收藏当前匹配帧”按钮，单个匹配保存当前结果，批量匹配默认保存分数最高的结果。
- 桌面端新增“收藏高分匹配”按钮，可一键保存当前匹配结果里达到阈值的帧。
- 桌面端新增“项目素材”区域，可统一查看搜索收藏和截图反查收藏。
- 桌面端“项目素材”区域新增类型筛选、排序和素材库导出按钮。
- 自动化测试使用 FFmpeg 生成自制 `testsrc` 视频和查询帧，不引入第三方版权素材。
- 新增可复跑脚本：`apps/local-api/scripts/stage2_robustness_check.py`。

## 鲁棒性测试

测试样本来自自制 `testsrc` 视频截图，并生成以下变体：

| 变形场景 | 综合分 | pHash 分 | 结果 |
| --- | ---: | ---: | --- |
| compressed | 0.871 | 1.0 | 通过 |
| scaled | 0.875 | 1.0 | 通过 |
| tone | 0.802 | 0.969 | 通过 |
| text | 0.824 | 1.0 | 通过 |
| occlusion | 0.756 | 0.938 | 通过 |

当前通过阈值为 `0.45`。这个阈值只用于 Mock Provider 阶段的流程验证，不能代表真实模型上线后的最终阈值。

## 测试结果

- API 服务与阶段2单元测试：13 个通过。
- FastAPI 路由加载检查：通过，已注册上传、批量匹配、匹配帧收藏、统一项目素材和素材库导出接口。
- 鲁棒性检查脚本：通过，5 个变形样本全部达到阈值。
- Electron 桌面端冒烟：通过，API 状态为 ready。

## 未完成

- OCR 仍为 Mock Provider，未接入 PaddleOCR 或其他真实 OCR。
- 视觉向量仍为 Mock Provider，未接入 CLIP 或其他真实 embedding 模型。
- 高分匹配一键收藏目前按当前阈值执行，尚未支持批量复核时逐条改状态。
- 统一项目素材视图尚未支持删除收藏记录；删除属于破坏性操作，需要明确授权后再开放。
- 真实平台候选视频采集尚未接入，当前候选视频仍需使用本地路径。

## 已知风险

- Mock OCR 和 Mock embedding 只能验证接口与流程，不能代表真实识别准确率。
- 当前 pHash 使用 FFmpeg 缩放为 8x8 灰度平均哈希，对裁剪、遮挡、字幕变化的鲁棒性有限。
- 当前匹配分数权重、阈值和精排窗口是阶段2临时实现，进入真实 Provider 前需要配置化和重新标定。
- 真实截图通常会有播放器控件、水印、压缩噪声、平台 UI 遮挡，后续需要加入更接近真实场景的自制样本。

## 下一步建议

- 明确授权后，再补统一项目素材视图的删除收藏记录功能。
- 补更完整的素材复核状态管理。
- 在接入真实 OCR Provider 前，先确认依赖体积、许可证、本机性能和离线可用性。

## 本次补充：素材复核状态管理

- 新增统一素材状态更新能力，支持把搜索收藏和截图反查收藏标记为 `pending`、`confirmed`、`rejected`。
- 新增接口：`POST /api/projects/{projectId}/library/{sourceType}/{materialId}/review-status`。
- 桌面端“项目素材”区新增状态筛选，列表卡片可直接切换“待复核 / 已确认 / 已拒绝”。
- 素材导出会跟随当前状态筛选，方便只导出待复核或已确认素材。
- 自动化测试增加到 14 个，覆盖普通搜索素材和截图反查素材的状态更新。

## 本次补充：素材标签和备注编辑

- 新增统一素材元信息更新能力，支持编辑搜索收藏和截图反查收藏的标签、备注。
- 新增接口：`POST /api/projects/{projectId}/library/{sourceType}/{materialId}/metadata`。
- 标签会自动去掉空值和重复值，并限制单条标签长度、标签数量和备注长度。
- 桌面端“项目素材”卡片新增标签输入框、备注输入框和保存按钮。
- 自动化测试增加到 15 个，覆盖两类素材的标签备注更新和基础输入校验。

## 本次补充：素材版权状态管理

- 新增统一版权状态更新能力，支持把搜索收藏和截图反查收藏标记为 `unknown`、`cleared`、`needs_permission`、`blocked`。
- 新增接口：`POST /api/projects/{projectId}/library/{sourceType}/{materialId}/rights-status`。
- 项目素材列表和素材导出新增 `rights_status` 筛选，方便只查看可用、需授权或不可用素材。
- 桌面端“项目素材”区新增版权筛选，素材卡片可直接切换“未知 / 可用 / 需授权 / 不可用”。
- 自动化测试增加到 16 个，覆盖两类素材的版权状态更新、筛选和导出。

## 本次补充：项目素材统计概览

- 项目素材列表返回值新增 `summary`，统计全部素材数、当前筛选数、来源类型、复核状态和版权状态。
- 桌面端“项目素材”区新增紧凑统计条，可直接看到待复核、已确认、已拒绝、可用、需授权、不可用数量。
- 统计概览基于项目全部素材计算，列表数量继续跟随当前筛选，方便区分“总池子”和“当前视图”。
- 自动化测试继续保持 16 个，并补充断言统计概览字段。

## 本次补充：项目素材关键词筛选

- 项目素材列表和导出新增 `keyword` 参数，可按标题、平台、来源、备注、标签和匹配类型筛选。
- 桌面端“项目素材”区新增关键词输入框，输入后自动刷新素材列表。
- 关键词筛选后，`summary.all_count` 仍表示项目全部素材数，`summary.filtered_count` 表示当前筛选命中数。
- 自动化测试增加到 17 个，覆盖按标签、备注、路径筛选和关键词导出。

## 本次补充：匹配帧时间码

- 项目素材列表为截图反查素材新增 `timecode`、`end_timecode` 和 `selected_timecode` 字段。
- 桌面端素材卡片显示 `00:00:01.250` 这类时间码，同时保留原始毫秒数。
- CSV/JSON/Markdown 导出新增时间码信息，方便后续剪辑定位。
- 关键词筛选支持按时间码搜索截图反查素材。

## 本次补充：项目素材按时间点排序

- 项目素材排序新增 `time_asc` 和 `time_desc`，可按截图反查素材的匹配时间点早晚排列。
- 桌面端“项目素材”排序下拉新增“时间点早在前 / 时间点晚在前”。
- 普通搜索收藏没有时间点，按时间点排序时会排在截图反查素材后面。
- 自动化测试增加到 18 个，覆盖时间点升序和降序排序。

## 本次补充：匹配证据摘要

- 截图反查收藏素材新增 `phash_score`、`visual_score`、`text_score`、`evidence` 和 `evidence_summary`。
- 证据摘要会显示粗扫帧数、精排帧数、粗扫 FPS、精排 FPS、阈值和粗扫最佳时间码。
- 桌面端项目素材卡片显示三路分数明细和证据摘要，方便判断匹配可信度。
- CSV/JSON/Markdown 导出新增分数明细和证据摘要，关键词筛选也支持按证据摘要搜索。

## 本次补充：匹配可信度标签

- 截图反查收藏素材新增 `match_confidence` 和 `match_confidence_label` 字段。
- 可信度按综合分划分：`high` 为高可信，`medium` 为中可信，`low` 为低可信。
- 桌面端素材卡片在匹配分旁边显示中文可信度，方便优先复核高价值素材。
- CSV/JSON/Markdown 导出包含可信度字段，关键词筛选也支持按“可信”检索。

## 本次补充：匹配可信度筛选

- 项目素材列表和导出新增 `match_confidence` 参数，支持 `all/high/medium/low`。
- 桌面端“项目素材”区新增可信度筛选，可只查看高可信、中可信或低可信截图反查素材。
- 普通搜索收藏没有匹配可信度，选择高/中/低筛选时不会出现在结果中。
- 自动化测试增加到 19 个，覆盖可信度筛选、导出和非法参数校验。

## 本次补充：可信度统计概览

- 项目素材 `summary` 新增 `by_match_confidence`，统计高可信、中可信、低可信截图反查素材数量。
- 桌面端统计条新增高/中/低可信数量，方便快速判断项目素材池质量。
- 统计仍基于项目全部素材计算，即使当前列表被筛选，也能看到全项目可信度分布。

## 本次补充：匹配片段时长

- 截图反查收藏素材新增 `duration_ms` 和 `duration_timecode`，由匹配开始/结束时间自动计算。
- 桌面端项目素材卡片显示片段时长，同时保留原始毫秒数。
- CSV/JSON/Markdown 导出新增片段时长信息，关键词筛选也支持按时长时间码检索。
