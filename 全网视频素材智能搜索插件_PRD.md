# 全网视频素材智能搜索插件 PRD

> 文档用途：直接交给 Codex 进行项目规划、开发、测试和验收  
> 产品形态：Windows 本地应用 + Chrome 浏览器扩展 + 本地 AI 搜索服务 + 可选 Codex/ChatGPT Skill  
> 当前版本：V1.0 需求基线  
> 目标用户：短视频、广告、AI 视频与内容创作团队

---

## 1. 项目名称

暂定名称：**全网视频素材智能搜索助手**

英文工程名建议：`video-material-finder`

## 2. 项目背景

内容创作者在制作短视频、广告、AI 视频、分镜和选题时，经常只记得一个画面、一句台词、一个关键词或一张截图，却不知道原始视频来自哪里。现有搜索方式需要分别打开小红书、抖音、微信视频号、B站以及普通搜索引擎，重复输入关键词，手工整理链接，效率低，且很难定位到视频中的具体时间点。

本项目需要提供一个统一入口，使用户可以通过以下任意信息查找相关内容：

- 一张视频截图；
- 一段画面描述；
- 一个或多个关键词；
- 一句字幕或台词；
- 一段短视频；
- 一个已有视频链接。

系统负责生成搜索词、跨平台搜索公开可访问内容、整理候选结果、识别相同或相似画面、定位对应视频时间点，并将结果保存到个人素材库。

## 3. 产品目标

### 3.1 核心目标

1. 用一个搜索框统一搜索多个平台的相关视频和公开资料。
2. 通过截图快速反查原视频、相似视频和跨平台转载版本。
3. 将截图定位到候选视频中的具体时间点。
4. 对搜索结果进行去重、分类、相似度排序和来源标记。
5. 为短视频项目建立可持续复用的本地素材索引库。
6. 默认保存原链接和分析信息，只对用户有权使用的内容执行下载。

### 3.2 非目标

第一版不承诺：

- 搜索互联网中的“所有”内容；
- 访问私密、付费、仅好友可见或已删除内容；
- 绕过验证码、登录限制、访问控制、DRM、加密或平台安全机制；
- 无人值守地大规模爬取平台；
- 判断或保证某个视频的绝对原创归属；
- 自动获得素材的商业使用授权；
- 自动发布、评论、点赞、关注或私信；
- 绕过平台限制下载禁止下载的视频。

产品对外应使用以下描述：

> 搜索公开可访问、当前可检索的相关视频与网页信息，并辅助定位可能的原始来源。

不得宣传为“保证搜到全网所有内容”或“保证找到原创作者”。

## 4. 用户场景

### 场景 A：截图反查原视频

用户上传一张包含字幕、水印、裁剪或调色的视频截图。系统识别画面和文字，生成搜索词，搜索多个平台，对候选视频抽取关键帧并匹配，最后返回可能的原视频、具体时间点和原始链接。

### 场景 B：用自然语言寻找素材

用户输入：

> 极端高温末日废墟里，一个男人翻找垃圾，最后发现一台旧空调。

系统拆分出场景、人物、动作、物体和情绪，并扩展中英文搜索词，返回相关视频、图片、文章、账号和话题。

### 场景 C：用台词找视频

用户输入一句台词或字幕。系统执行精确短语搜索、近义改写搜索和 OCR/ASR 字幕索引匹配，返回包含该台词或近似表达的视频。

### 场景 D：剧本批量匹配素材

用户粘贴分镜脚本。系统按镜头拆分需求，为每个镜头生成候选素材列表，并导出素材清单。该能力安排在后续版本。

### 场景 E：跨平台溯源

系统发现同一段画面出现在多个平台，按发布时间、清晰度、时长完整性、是否有二次水印等信息进行比较，给出“更可能接近原始来源”的推测和证据，禁止在证据不足时直接认定原创者。

## 5. 支持平台与实现边界

| 平台 | V1 支持方式 | 自动化程度 | 说明 |
|---|---|---:|---|
| B站 | 公开网页搜索、浏览器辅助、结果解析 | 高 | 作为第一优先平台 |
| 小红书 | 用户扫码登录后的浏览器辅助搜索 | 中 | 使用用户本机浏览器登录态，限制频率 |
| 抖音 | 官方开放能力优先，浏览器辅助为补充 | 中 | 不依赖单一非官方接口 |
| 微信视频号 | 关键词生成、人工搜索、当前页面/播放内容收藏与匹配 | 低至中 | V1 不做全自动全站搜索 |
| 普通网页 | 搜索引擎结果、公开网页解析 | 高 | 搜索新闻、文章、图片与视频页面 |
| YouTube/TikTok | V2 接入 | 中至高 | 先预留适配器接口 |
| Pexels/Pixabay 等 | V2 接入 | 高 | 优先展示授权信息明确的素材 |

每个平台必须实现独立适配器。单个平台失效时，不得导致整个搜索任务失败。

## 6. 产品组成

### 6.1 Windows 本地应用

负责：

- 统一搜索界面；
- 截图、视频片段和链接输入；
- 搜索任务管理；
- 候选结果展示；
- 图片与视频分析；
- 本地数据库和素材库；
- 设置、日志和错误提示；
- 导出搜索结果。

### 6.2 Chrome 浏览器扩展

负责：

- 使用用户主动登录后的浏览器会话；
- 打开平台搜索页；
- 将关键词填入搜索框；
- 在用户可见页面中读取公开结果；
- 收集当前页面的视频标题、作者、封面、链接等；
- 将用户主动打开的视频添加到本地素材库；
- 不读取密码，不上传完整 Cookie，不隐蔽执行高频操作。

### 6.3 本地后端服务

负责：

- 搜索任务调度；
- OCR、ASR、关键帧抽取；
- 图片指纹和视觉向量计算；
- 候选排序、去重和时间点定位；
- 数据库存储；
- 本地 API；
- 任务进度和日志。

### 6.4 Codex/ChatGPT Skill

后续版本提供，负责：

- 把自然语言需求转换为结构化搜索任务；
- 扩展中英文关键词；
- 调用本地服务；
- 汇总候选素材；
- 根据剧本生成逐镜头素材清单。

Skill 本身不直接实现平台爬取和视频分析，核心能力由本地服务提供。

## 7. V1 功能需求

### 7.1 首页与统一搜索框

支持以下输入类型：

- 纯文本关键词；
- 自然语言描述；
- 台词或字幕；
- PNG/JPG/WebP 截图；
- MP4/MOV 短视频片段；
- 视频或网页 URL。

搜索设置：

- 平台多选；
- 内容类型：视频、图片、文章、账号；
- 时间范围；
- 横屏、竖屏、不限；
- 完全相同、相似内容、全部；
- 是否优先授权明确素材；
- 每个平台最大结果数；
- 搜索语言：中文、英文、中英双语。

### 7.2 查询理解与关键词扩展

系统需要从自然语言中提取：

- 场景；
- 人物；
- 动作；
- 物体；
- 地点；
- 时间与天气；
- 情绪；
- 画面风格；
- 镜头类型；
- 精确台词；
- 排除词。

系统应生成：

- 原始关键词；
- 精确短语；
- 同义词；
- 口语表达；
- 平台常用标签；
- 英文翻译及英文近义词；
- 2至10组搜索组合。

关键词扩展必须允许用户查看和编辑。

### 7.3 平台搜索任务

统一任务状态：

- waiting；
- running；
- needs_user_action；
- completed；
- partial_success；
- failed；
- cancelled。

每个平台任务需要：

- 独立超时；
- 独立错误处理；
- 限速；
- 最大翻页数；
- 结果数量上限；
- 登录过期提示；
- 验证码或人工操作提示；
- 可取消；
- 可重试，但不得无限重试。

### 7.4 搜索结果采集

尽可能获取以下字段：

- 平台；
- 标题；
- 作者名称；
- 作者主页链接；
- 原始内容链接；
- 封面链接或本地缩略图；
- 发布时间；
- 时长；
- 描述；
- 标签；
- 点赞、评论、转发等公开数据；
- 搜索关键词；
- 采集时间；
- 是否需要登录；
- 是否允许平台内下载；
- 版权状态备注。

字段无法获得时允许为空，禁止伪造。

### 7.5 截图分析

上传截图后执行：

1. 文件格式和大小校验；
2. 生成原图缩略图；
3. OCR 识别字幕、用户名、水印、招牌和其他文字；
4. 检测画面主体、场景、动作、物体和风格；
5. 计算感知哈希；
6. 计算视觉向量；
7. 生成关键词候选；
8. 用户确认或直接开始搜索。

需要适应：

- 图片裁剪；
- 画面缩放；
- 轻度旋转；
- 压缩；
- 加字幕；
- 加边框；
- 水印；
- 轻度调色；
- 局部遮挡。

### 7.6 候选视频关键帧匹配

对允许读取或用户主动提供的候选视频：

- 使用 FFmpeg 获取视频基础信息；
- 基于场景变化抽取关键帧；
- 必要时按固定间隔补充抽帧；
- 先使用感知哈希进行快速筛选；
- 再使用视觉向量进行精排；
- 对最高候选在邻近时间范围进行更密集抽帧；
- 输出最佳匹配时间点和时间区间；
- 保存匹配关键帧用于结果对比。

匹配结果至少包含：

- 候选视频；
- 匹配时间点；
- 相似度；
- 匹配类型：相同画面、疑似同源、视觉相似；
- 输入截图与候选帧对比；
- 匹配证据说明。

### 7.7 OCR 与 ASR

OCR 用于：

- 截图文字识别；
- 视频关键帧字幕识别；
- 水印和账号线索识别。

ASR 用于：

- 用户上传短片的语音转文字；
- 有权分析的候选视频语音转文字；
- 台词时间轴搜索。

ASR Provider 采用可替换接口，至少预留：

- 本地 Whisper；
- 豆包 ASR；
- Mock Provider，用于无密钥测试。

### 7.8 去重和同源判断

去重信号包括：

- 标准化 URL；
- 视频平台 ID；
- 标题相似度；
- 作者；
- 封面感知哈希；
- 关键帧相似度；
- 时长；
- OCR/ASR 文本相似度。

同源判断输出等级：

- exact_duplicate：完全重复；
- probable_repost：可能转载；
- same_source_clip：疑似同一来源的不同剪辑；
- visually_similar：仅画面或主题相似；
- unrelated：不相关。

### 7.9 排序

默认综合评分建议：

`总分 = 画面相似度 + 文字相似度 + 查询语义相似度 + 来源可信度 + 时间线证据 + 结果完整度`

具体权重必须配置化，不得硬编码在前端。

结果支持按以下方式排序：

- 综合相关度；
- 画面相似度；
- 发布时间；
- 热度；
- 时长；
- 授权清晰程度。

### 7.10 素材库

用户可以：

- 收藏搜索结果；
- 创建项目；
- 添加标签；
- 添加备注；
- 保存匹配时间点；
- 查看原始链接；
- 标记已确认、待确认、不相关；
- 标记版权状态；
- 删除本地索引；
- 导出 CSV/JSON/Markdown。

默认保存：元数据、缩略图、关键帧、分析结果和原始链接。

默认不保存完整视频文件。

### 7.11 版权状态

每条素材显示：

- 授权明确可商用；
- 需署名；
- 需获得作者授权；
- 平台内可查看但用途不明；
- 版权状态未知；
- 用户已确认拥有使用权。

系统不得仅凭“可下载”判断“可商用”。

### 7.12 日志与错误提示

用户可理解的错误类型：

- 未登录；
- 登录已过期；
- 需要扫码；
- 需要人工验证码；
- 平台暂时限制访问；
- 页面结构已变化；
- 网络失败；
- 视频不可访问；
- 未找到结果；
- 分析模型不可用；
- 本地空间不足。

开发日志不得记录用户密码、完整 Cookie、令牌或其他敏感凭证。

## 8. 页面需求

### 8.1 首页

- 大型统一搜索框；
- 拖拽上传截图/视频；
- 平台选择；
- 高级筛选；
- 最近搜索；
- 最近项目。

### 8.2 搜索任务页

- 总进度；
- 各平台独立进度；
- 当前搜索词；
- 已找到数量；
- 登录/扫码/验证码操作提示；
- 暂停、继续、取消。

### 8.3 搜索结果页

- 网格和列表两种视图；
- 左侧筛选；
- 相似度、平台、时间等排序；
- 原截图与匹配帧对比；
- 原链接；
- 添加到素材库；
- 不相关反馈。

### 8.4 素材详情页

- 基础信息；
- 匹配时间点；
- 关键帧时间轴；
- OCR/ASR 结果；
- 同源候选；
- 版权备注；
- 项目与标签；
- 用户备注。

### 8.5 素材库页

- 项目树；
- 标签筛选；
- 搜索历史；
- 收藏内容；
- 导出。

### 8.6 设置页

- 平台登录状态；
- 搜索数量和频率限制；
- OCR/ASR/视觉模型设置；
- 数据存放位置；
- 缓存大小；
- 代理设置，仅支持用户明确配置的合法代理；
- 隐私和数据清理；
- 诊断信息。

## 9. 技术架构建议

### 9.1 技术栈

| 层级 | 推荐技术 |
|---|---|
| 桌面应用 | Electron + React + TypeScript |
| UI组件 | Ant Design 或同类成熟组件库 |
| 状态管理 | Zustand |
| 本地后端 | Python 3.11+ + FastAPI |
| 浏览器辅助 | Chrome Extension Manifest V3 |
| 必要的可见浏览器流程 | Playwright，仅在用户授权的本机环境运行 |
| 视频处理 | FFmpeg/ffprobe |
| OCR | PaddleOCR，封装 Provider 接口 |
| ASR | Whisper/豆包 ASR，封装 Provider 接口 |
| 图片处理 | OpenCV + Pillow |
| 感知哈希 | imagehash 或等价实现 |
| 视觉向量 | CLIP 系列本地模型，封装 Provider 接口 |
| 本地数据库 | SQLite + SQLAlchemy/Alembic |
| 向量索引 | FAISS 或 SQLite 向量扩展 |
| 后端测试 | pytest |
| 前端测试 | Vitest + Playwright E2E |
| 打包 | Electron Builder |

所有模型和平台适配器必须通过接口解耦，避免供应商或页面变化导致全工程重写。

### 9.2 模块目录建议

```text
video-material-finder/
├── apps/
│   ├── desktop/              # Electron + React
│   ├── browser-extension/    # Chrome MV3
│   └── local-api/            # FastAPI
├── packages/
│   ├── shared-types/
│   ├── search-query/
│   └── ui/
├── services/
│   ├── platform-adapters/
│   │   ├── bilibili/
│   │   ├── xiaohongshu/
│   │   ├── douyin/
│   │   ├── wechat-channels/
│   │   └── web-search/
│   ├── image-matching/
│   ├── video-analysis/
│   ├── ocr/
│   ├── asr/
│   └── ranking/
├── tests/
│   ├── fixtures/
│   ├── integration/
│   └── e2e/
├── docs/
├── scripts/
├── LICENSES/
├── .env.example
├── docker-compose.dev.yml
└── README.md
```

### 9.3 平台适配器接口

每个平台实现统一能力描述：

```ts
interface PlatformAdapter {
  platform: PlatformName;
  capabilities(): PlatformCapabilities;
  checkSession(): Promise<SessionStatus>;
  startSearch(query: SearchQuery, options: SearchOptions): Promise<SearchTask>;
  continueSearch(taskId: string, cursor?: string): Promise<SearchPage>;
  normalizeResult(raw: unknown): SearchResult;
  cancel(taskId: string): Promise<void>;
}
```

适配器不得假设所有平台都支持自动搜索、翻页、下载或获取视频文件。通过 `capabilities()` 明确声明能力。

## 10. 数据模型

### 10.1 SearchTask

- id
- input_type
- original_query
- expanded_queries
- selected_platforms
- filters_json
- status
- progress
- created_at
- started_at
- finished_at
- error_summary

### 10.2 SearchResult

- id
- task_id
- platform
- platform_content_id
- content_type
- title
- description
- author_name
- author_url
- source_url
- cover_url
- local_thumbnail_path
- published_at
- duration_ms
- public_metrics_json
- matched_query
- collected_at
- availability_status
- rights_status
- raw_metadata_json

### 10.3 MediaAnalysis

- id
- result_id
- ocr_text
- asr_text
- detected_entities_json
- perceptual_hash
- embedding_model
- embedding_ref
- analysis_version
- created_at

### 10.4 FrameMatch

- id
- query_asset_id
- result_id
- timestamp_ms
- end_timestamp_ms
- match_type
- phash_score
- visual_score
- text_score
- combined_score
- local_frame_path
- evidence_json

### 10.5 ProjectMaterial

- id
- project_id
- result_id
- selected_timestamp_ms
- tags_json
- note
- review_status
- rights_status
- created_at

## 11. 本地 API 建议

```text
POST   /api/search/tasks
GET    /api/search/tasks/{id}
POST   /api/search/tasks/{id}/cancel
POST   /api/search/tasks/{id}/resume
GET    /api/search/tasks/{id}/results

POST   /api/assets/analyze-image
POST   /api/assets/analyze-clip
POST   /api/matches/find
GET    /api/matches/{id}

GET    /api/platforms
GET    /api/platforms/{platform}/session
POST   /api/platforms/{platform}/start-login

POST   /api/projects
GET    /api/projects
POST   /api/projects/{id}/materials
GET    /api/projects/{id}/materials

GET    /api/exports/{taskId}.csv
GET    /api/exports/{taskId}.json
GET    /api/exports/{taskId}.md
```

## 12. 安全、隐私与合规要求

1. 所有平台登录必须由用户在真实可见页面中完成。
2. 不在服务器存储平台密码。
3. Cookie 或登录态默认仅保存在用户本机，并使用系统安全存储能力保护。
4. 不将登录态写入日志、数据库导出或错误报告。
5. 不实现验证码绕过、设备指纹伪造、DRM破解、加密视频解密或访问控制绕过。
6. 遇到平台验证时将任务切换为 `needs_user_action`。
7. 默认低频、小批量、用户发起搜索，不设计大规模后台采集。
8. 遵守各平台服务条款、robots约束、适用法律和内容版权规则。
9. 第三方开源代码进入项目之前必须审查许可证。
10. 禁止复制仅限非商业用途的代码进入商业项目。
11. 没有明确许可证的仓库仅可作为调研参考，不复制其代码。
12. 对用户导入或下载的素材显示版权提醒，并保留来源链接。

## 13. 性能要求

V1 基线：

- 应用冷启动：普通开发电脑上目标小于15秒；
- 单张截图本地初步分析：目标小于10秒，不含模型首次下载；
- 已建立关键帧索引的本地素材库截图匹配：10000帧内目标小于5秒；
- 搜索结果到达后应流式展示，不等待所有平台结束；
- 单个平台失败不影响其他平台结果；
- 后台分析任务不得阻塞桌面界面；
- 默认缓存上限可配置；
- 数据库升级使用迁移脚本。

以上为目标值，Codex 应在真实测试后记录基线，不得伪造性能结论。

## 14. V1 验收标准

### 14.1 关键词搜索

- 输入一段自然语言后能够生成可编辑的关键词组；
- 至少两个平台适配器能够返回真实公开结果；
- 搜索结果包含标题、平台、原链接和封面中的至少三项；
- 单个平台失败时其他平台继续运行；
- 能保存搜索历史和导出结果。

### 14.2 截图反查

- 支持 PNG/JPG/WebP；
- 能识别截图中的主要文字；
- 能计算图片指纹和视觉向量；
- 对测试集中的原始帧能够找到对应候选视频；
- 能输出候选时间点和对比帧；
- 对裁剪、加字幕、轻度调色和压缩后的截图进行鲁棒性测试；
- 未达到阈值时明确显示“未找到可靠匹配”，不得强行返回确定结论。

### 14.3 素材库

- 能创建项目；
- 能收藏结果；
- 能保存标签、备注和时间点；
- 能打开原始链接；
- 能导出 CSV、JSON 或 Markdown 中至少两种格式；
- 删除本地记录需要确认，且不删除平台原内容。

### 14.4 安全

- 日志中无密码、完整 Cookie、Token；
- 登录流程由用户可见地完成；
- 无验证码绕过代码；
- 无代理证书拦截、加密视频解密或 DRM 绕过；
- 依赖和许可证清单存在并可审计。

## 15. 测试要求

### 15.1 单元测试

- 查询拆分和关键词扩展；
- URL 标准化；
- 平台结果归一化；
- 感知哈希相似度；
- OCR 文本清理；
- 综合评分；
- 去重规则；
- 权限和版权状态显示。

### 15.2 集成测试

- 桌面端到本地 API；
- 浏览器扩展到本地 API；
- FFmpeg 抽帧；
- OCR Provider；
- ASR Mock Provider；
- SQLite迁移；
- 平台适配器使用录制样本或Mock，避免CI高频访问真实平台。

### 15.3 E2E 测试

- 文本搜索完整流程；
- 截图搜索完整流程；
- 平台登录过期流程；
- 人工验证暂停和恢复；
- 收藏到素材库；
- 导出结果；
- 单个平台失败后的部分成功状态。

### 15.4 测试素材

测试仓库只使用：

- 自制视频；
- 公共领域内容；
- 明确允许测试使用的素材；
- 本地生成的变体截图。

不得把受版权保护的完整第三方视频提交到 Git 仓库。

## 16. 开发阶段与阶段门禁

### 阶段 0：仓库与技术验证

交付：

- GitHub 私有仓库；
- Monorepo基础结构；
- 许可证审查清单；
- FFmpeg、OCR、pHash、视觉向量最小验证；
- 一个B站或公开网页搜索适配器的可行性验证；
- 技术风险报告。

门禁：上述验证通过前，不开发完整UI。

### 阶段 1：本地素材搜索 MVP

交付：

- Electron桌面壳；
- FastAPI本地服务；
- SQLite数据库；
- 关键词搜索；
- 至少两个可用平台适配器；
- 结果列表；
- 收藏与项目；
- 基础导出。

### 阶段 2：截图反查

交付：

- 截图上传；
- OCR；
- pHash；
- 视觉向量；
- 候选视频抽帧；
- 精确到时间点的匹配；
- 对比界面；
- 变体鲁棒性测试报告。

### 阶段 3：浏览器扩展与平台登录

交付：

- Chrome MV3扩展；
- 用户主动登录；
- 页面结果收集；
- 登录状态检查；
- 需要人工操作时暂停和恢复；
- 小红书、抖音适配完善。

### 阶段 4：视频号半自动流程

交付：

- 视频号搜索词生成；
- 用户人工搜索说明；
- 当前打开或主动提供内容的采集；
- 与截图的匹配；
- 不包含代理证书、解密或拦截方案。

### 阶段 5：生产化

交付：

- Windows安装包；
- 自动更新策略；
- 数据备份与迁移；
- 性能测试；
- 安全审查；
- 用户使用说明；
- Codex/ChatGPT Skill；
- 剧本批量匹配原型。

## 17. Codex 执行规则

将本PRD交给Codex后，Codex必须遵循：

1. 开始前检查当前目录、Git状态、现有代码和项目说明。
2. 若项目为空，先输出技术方案和阶段0计划，不直接生成全部工程。
3. 对GitHub候选依赖逐一检查许可证、维护状态和必要性。
4. 未获得明确授权不得复制非商业许可证或无许可证仓库代码。
5. 每次只实施当前阶段，完成测试并通过门禁后再进入下一阶段。
6. 所有平台功能通过适配器接口实现。
7. 所有AI能力通过Provider接口实现，并提供Mock。
8. 不将密码、Cookie或Token写入代码、日志、测试快照或Git。
9. 不使用真实账号进行无人值守高频测试。
10. 不实现验证码绕过、DRM破解、视频号代理拦截或加密解密。
11. 对页面结构变化提供清晰错误，不静默失败。
12. 所有结论必须来自真实测试输出，不得宣称未经验证的功能已完成。
13. 每个阶段结束时输出：已完成、未完成、测试结果、已知风险、下一阶段建议。
14. Git提交保持小而清晰；推送、创建远程仓库和PR前必须获得用户明确授权。
15. Windows是主要交付平台，所有命令和路径同时考虑Windows兼容性。

## 18. 建议的第一条 Codex 启动指令

将本文件放入项目根目录后，对Codex发送：

```text
请阅读项目根目录的《全网视频素材智能搜索插件_PRD.md》。

这是一个新的Windows本地项目。先只执行“阶段0：仓库与技术验证”，不要提前开发完整产品功能。

要求：
1. 检查当前目录、Git状态和已有文件。
2. 根据PRD提出技术架构、依赖选择和风险清单。
3. 对拟使用的开源依赖检查许可证，禁止复制非商业或无许可证仓库代码。
4. 建立最小Monorepo结构。
5. 分别验证FFmpeg抽帧、OCR、pHash、视觉向量和一个公开平台搜索适配器。
6. 为验证代码编写自动化测试。
7. 输出真实执行结果、失败项、阻塞项和下一阶段建议。
8. 未经我明确同意，不创建远程仓库、不推送GitHub、不进入阶段1。
```

## 19. 已知风险

| 风险 | 影响 | 缓解方案 |
|---|---|---|
| 平台页面频繁变化 | 适配器失效 | 独立适配器、录制样本测试、快速更新 |
| 登录风控和验证码 | 搜索中断 | 低频、用户可见操作、人工恢复 |
| 视频号开放能力有限 | 无法全自动搜索 | V1采用半自动流程 |
| 搜索结果无法读取视频 | 无法精确抽帧 | 先返回链接；仅对用户提供或允许读取的内容分析 |
| 截图改动过大 | 匹配准确率下降 | OCR+pHash+视觉向量+多信号排序 |
| 同源与原创判断错误 | 版权风险 | 只给证据和概率，不下绝对结论 |
| 开源项目许可证限制 | 无法商用 | 依赖审查、独立实现、保留许可证清单 |
| 本地模型占用资源 | 低配电脑运行慢 | 模型可替换、队列化、低配模式 |
| 搜索范围过大 | 时间长、结果噪音高 | 平台/数量/时间筛选、分批展示 |

## 20. 后续版本候选

- YouTube、TikTok和更多公开素材库；
- 以图搜图服务适配；
- 剧本逐镜头匹配；
- 自动生成剪映/PR素材清单；
- 团队共享素材库；
- 素材授权证据附件；
- 相似镜头聚类；
- 自动识别景别、机位、运镜和构图；
- 基于用户反馈调整排序；
- 仅在获得授权后下载并截取指定片段；
- Codex/ChatGPT Skill自然语言入口。

---

## 最终产品原则

这个产品的价值不是“大规模下载全网视频”，而是：

> 让创作者通过一张截图、一句台词或一个画面描述，更快找到公开来源、相似素材、具体时间点和可复用的创作线索。

准确、可追溯、用户可控和合规优先于采集数量。
