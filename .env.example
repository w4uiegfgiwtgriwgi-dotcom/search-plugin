# Environment sample for local development only.
# Do not store passwords, full cookies, tokens, or platform login state here.

APP_ENV=development
LOCAL_API_PORT=17860
DATA_DIR=./.local-data
LOG_LEVEL=info
