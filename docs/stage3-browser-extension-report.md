# 阶段3浏览器扩展打通报告

> 日期：2026-08-10  
> 范围：阶段3第一小步，Chrome MV3 扩展把用户主动采集的当前页面保存到本地 API。  
> 边界：不读取密码、Cookie、Token 或登录态；不做后台批量采集；不自动翻页；不绕过验证码或平台限制。

## 已完成

- 新增本地 API：`POST /api/browser/collect-page`。
- 扩展弹窗点击后读取当前页面公开信息：标题、URL、作者 meta、描述 meta。
- 扩展将页面信息发送到 `127.0.0.1:17860` 本地 API。
- 本地 API 自动创建或复用“浏览器采集素材”项目。
- 采集页面会写入 `search_results`，来源标记为 `browser-extension`。
- 页面会同步收藏到项目素材库，并自动添加“浏览器采集”标签。
- 服务层测试覆盖浏览器页面采集入库和非法 URL 拦截。

## 未完成

- 尚未针对 B站、小红书、抖音、视频号做页面结构专用解析。
- 尚未从页面读取封面、视频时长、发布时间、播放量等平台字段。
- 尚未在桌面端做“来自浏览器扩展”的单独入口或实时通知。
- 尚未做扩展手动加载、真实网页点击采集的人工验收记录。

## 下一步建议

- 给浏览器采集入口补平台字段解析，先从通用 `og:*`、`twitter:*` meta 开始。
- 桌面端素材库增加来源提示和刷新提示，方便用户采集后马上看到素材。
- 做一次 Chrome 扩展手动加载验收：打开公开网页，点击扩展，确认本地素材库出现该页面。

## 本次补充：浏览器采集字段补强

- 扩展新增读取 `og:image`、`twitter:image`、`article:published_time`、`og:site_name` 等公开 meta 字段。
- 本地 API 保存浏览器采集页面的封面、发布时间、站点名和主机名。
- 项目素材库中浏览器采集素材新增 `collection_source_label`，显示为“浏览器采集”。
- 桌面端素材卡片显示站点、作者和发布时间，方便区分普通搜索收藏和浏览器主动采集。

## 本次补充：扩展选择保存项目

- 扩展弹窗新增项目下拉框，会从本地 API 读取当前项目列表。
- 保存当前页面时会带上选中的 `project_id`，页面素材进入指定项目。
- 如果没有选择项目，仍会自动创建或复用“浏览器采集素材”项目。
- 保存按钮在请求期间会临时禁用，避免重复点击造成重复采集。

## 本次补充：扩展刷新项目列表

- 扩展弹窗新增“刷新项目”按钮，可重新读取本地项目列表。
- 刷新时会尽量保留当前已选择的项目。
- 刷新请求期间按钮会临时禁用，避免重复请求。
- 当本地 API 不可用时，弹窗会显示可读错误提示。

## 本次补充：阶段3手动验收准备

- 新增 `docs/stage3-manual-acceptance.md`，说明 Chrome 扩展加载、保存页面和桌面端确认步骤。
- 新增 `apps/local-api/scripts/stage3_browser_collect_smoke.py`，可模拟扩展采集公开页面并验证本地 API 入库链路。
- 扩展 README 增加手动验收文档入口。
- 这一步仍不读取密码、Cookie、Token 或登录态，只验证用户主动采集公开页面。

## 本次补充：桌面端浏览器采集提示

- 桌面端项目素材区新增“浏览器扩展采集”提示条。
- 新增“只看浏览器采集”按钮，会自动按“浏览器采集”关键词筛选素材。
- 新增浏览器采集提示区的“刷新素材”按钮，方便扩展保存页面后马上刷新列表。
- 项目素材关键词搜索新增采集来源、站点、主机名、作者和发布时间字段。
